# RTP implementation verification

Software checks on 2026-09-05 used laptop PipeWire 1.4.2 and the Linux Flutter toolchain in this workspace. The pre-existing prototype and its measurement evidence were preserved. The initial checks used isolated fixtures. A subsequent setup check installed and configured the receiver package on the physical Pi without starting RTP, as recorded below.

| Check | Observed result |
| --- | --- |
| New Python runtime and package tests | 55 passed, including packet filtering, state transitions, stale unmute, UDP/control loss, muted startup interruption, bounded commands, app timeout, actual guardian pipe death, actual sender process termination, matching route restoration, ambiguous journals, stale cleanup isolation, package fixtures, discovery defaults and retaining status alongside errors |
| Existing prototype and measurement tests | 33 passed, including the new readiness/operator marker timing test |
| Full Flutter test suite | 60 passed, including delayed mute confirmation, truthful one second unconfirmed UI, stale generation and delayed poll handling, explicit startup, mute during blocked gain work, editable discovery defaults, inline validation, saved receiver selection and stale discovery responses |
| Existing audio control shell fixtures | 43 assertions passed |
| Flutter analysis | No issues |
| Isolated production transport smoke | 400 packets in one second; 120 frames and 492 bytes per packet; 568 total packets accepted during the sampled run with no rejections; receiver muted at 10%; negotiated virtual output quantum 128; native RTP socket verified on loopback |
| Production desktop smoke | 1199, 1204 and 1203 packets in successive three second stages; nonzero audio continued after sink and source inspection; peak 328 S16 units; default desktop route unchanged |
| systemd validation | Receiver services/socket and the laptop sender user service passed `systemd-analyze verify` in their respective scopes |
| Linux release build | Flutter release bundle built for x64 |
| Debian builds | Laptop amd64 package and separate architecture independent receiver package built; receiver contents checked to exclude experiments, recordings and laptop helpers |
| Physical receiver setup | Receiver package 1.0.0 installed on `hifiberry`, configured for `syren-player-1` at `192.168.2.27`; new SSH login has control group access; socket active; receiver preflight passed with PipeWire 1.4.2, required modules and device present; Snapclient remained active |
| Host discovery | Fresh preferences resolved discovered name `hifiberry` to `hifiberry.local` at `192.168.2.27`; login suggestion `yme` remained editable; existing verified pairing retained |

Package tests execute the real maintenance hooks against isolated old helper and systemctl fixtures, for muted and audible simulated states. They verify that installation does not start RTP, upgrades and removals drain first, interrupted restoration returns failure, and the helper and recovery journal remain available for retry. They do not simulate kernel scheduling, physical ALSA release timing or all distribution package manager behavior. The receiver's cgroup binding and `ExecStopPost` are structurally validated; physical broker crash and package action exercises are still required.

The 50 ms monitor cadence, 250 ms loss threshold, further 250 ms mute deadline, one second unconfirmed UI, three second control/app heartbeat deadlines and 12 second lease are implemented. Unit tests validate their decisions and local control interruption paths. They are not measurements of worst case Pi response under load. The new ingress filter's overhead has not been calibrated acoustically.

Release acceptance remains **outstanding**. Fresh comparable local, Snapcast and filtered RTP measurements must establish median added delay at most 30 ms and p95 at most 50 ms, with calibrated uncertainty. Measure physical picture to sound offset, check for audible glitches and growing delay, run ten minute 20/15/10 ms candidates and 30/40 ms fallback if needed, exercise failures and package actions on the physical receiver, and complete two hours of live F1. Earlier screen capture offsets and the quiet clip's 80% calibration do not establish these results. Software completion authorizes the opt in feature only.

On 2026-09-06 the speaker controls were simplified to one low latency switch on an existing configured speaker. Tests cover enabling through initial readiness, cancelling delayed startup, refusing to reuse enable intent after recovery, confirmed volume steps, and routing the existing group volume and mute controls to the active receiver. Full Flutter tests (60) and runtime Python tests (55) passed. The switch starts playback only after an explicit user gesture; install, restart and recovery do not authorize unmute. This UI change does not add latency or acoustic validation evidence.

The group and speaker slider regression also checks independent persisted levels during RTP: 60% group and 50% speaker produce 30% receiver gain; changing speaker level leaves the group unchanged, a zero speaker level remains silent through group changes, and saving an unchanged group editor does not send a gain command.

Startup volume regression checks on 2026-09-06: all 66 Flutter tests passed. The initial enable applies and confirms the saved group master volume multiplied by speaker level before unmuting. Tests cover zero volume, configured mute, and stop, mute, recovery or failed gain readback during initialization. These checks add no acoustic measurement evidence.

Group priority and notification checks on 2026-09-06: all 78 Flutter tests passed and Flutter analysis found no issues. Existing group source order drives RTP ownership handoffs, using server activity and running, unmuted desktop output streams. Regression checks cover both source orders, pause and return, reordered or removed sources, saved gain and mute on return, explicit disable, recovery between polls, server loss during inspection, and failed activity inspection. Notifications replace queued messages and clear the navigation bar. Physical source handoff timing and audible continuity still require live validation.

Shared output verification on 2026-09-06: 79 Flutter tests and 59 Python runtime/package tests passed; Flutter analysis and the shell routing checks passed. The existing production RTP and desktop smoke checks passed. The new shared output smoke uses two synthetic frequencies, real private Pulse and RTP modules, a simulated Snapclient frontend, and a virtual hardware sink. It confirmed source isolation, effective RTP gain, silence on group mute, advancing muted packets with no old audio after return, and unchanged graph/frontend process identities across source changes. It does not establish real Snapclient playback quality, physical handoff time, added acoustic delay, or synchronized group timing. Those live checks remain outstanding.

On 2026-09-07 recovery handling changed so the switch intent survives a receiver rebuild. The recovered receiver still comes back muted at 10%; the coordinator then confirms the saved volume again and unmutes, or selects standby when Spotify has priority. A pending startup gesture is still discarded, an explicit mute during recovery suppresses the automatic resume, and a failed volume confirmation during resume drops the intent instead of retrying. The speaker switch now says **Muted** only when the selected output is muted, and otherwise **Reconnected · Resuming** or **Paused · Snapcast selected**. Saving the group editor with the mute off resumes a paused session with a confirmed ramp, and an unchanged save during a Spotify handoff sends nothing. Flutter tests (90) and runtime Python tests (65) passed. These checks add no acoustic or latency evidence.
